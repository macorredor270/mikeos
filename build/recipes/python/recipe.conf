# Receta MPM: Python (build estándar precompilado, python-build-standalone)
NAME="python"
VERSION="3.12.14"
SOURCE_URL="https://github.com/astral-sh/python-build-standalone/releases/download/20260825/cpython-3.12.14%2B20260825-x86_64-unknown-linux-gnu-install_only_stripped.tar.gz"
SOURCE_SHA256="7ce4a71285d913955a76053cc7605ea96da8ecada54dba9cf395245961816421"
BUILD_CMD=""
INSTALL_CMD='mkdir -p "$DESTDIR$PREFIX" && for d in bin lib include share; do [ -d "$d" ] && cp -a "$d" "$DESTDIR$PREFIX/"; done && ln -sf python3.12 "$DESTDIR$PREFIX/bin/python3" && ln -sf python3.12 "$DESTDIR$PREFIX/bin/python"'
DEPENDS=""
