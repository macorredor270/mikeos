NAME="go"
VERSION="1.27.0"
SOURCE_URL="https://go.dev/dl/go1.27.0.linux-amd64.tar.gz"
SOURCE_SHA256="675c26c449cbb18fc24b74650de1eabbae6e16f64326fd85a283fb3b58280685"
BUILD_CMD=""
INSTALL_CMD='mkdir -p "$DESTDIR$PREFIX/lib" && cp -a . "$DESTDIR$PREFIX/lib/go/" && mkdir -p "$DESTDIR$PREFIX/bin" && ln -sf ../lib/go/bin/go "$DESTDIR$PREFIX/bin/go" && ln -sf ../lib/go/bin/gofmt "$DESTDIR$PREFIX/bin/gofmt"'
DEPENDS=""
